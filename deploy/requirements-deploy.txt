# Dimensio 部署依赖
# 用于生产环境的额外依赖

# WSGI 服务器
gunicorn==21.2.0

# 监控和日志
supervisor==4.2.5  # 进程管理（可选）

# 性能优化
gevent==23.9.1  # 异步支持（可选）

# 如果需要使用 PostgreSQL
# psycopg2-binary==2.9.9

# 如果需要使用 Redis
# redis==5.0.1

# 如果需要使用 Celery（异步任务）
# celery==5.3.4
