# Dimensio Deployment Environment Variables
#
# 📝 使用说明:
#   - 本地开发: 复制 .env.local 并根据需要修改
#   - 生产环境: 复制 .env.production 并修改 SERVER_IP
#
# 💡 快速开始:
#   本地开发: bash deploy-local.sh
#   生产环境: sudo bash deploy-production.sh

# ============================================================================
# 环境类型
# ============================================================================
# 可选值: local, production
ENV=production

# ============================================================================
# 服务器配置
# ============================================================================
# 生产环境: 修改为你的服务器 IP 或域名
SERVER_IP=8.140.237.35
SERVER_NAME=8.140.237.35

# 本地开发: 使用 localhost
# SERVER_IP=localhost
# SERVER_NAME=localhost

# 项目目录
PROJECT_DIR=/root/dimensio

# ============================================================================
# 应用配置
# ============================================================================
FLASK_APP=api/server.py
FLASK_ENV=production
PYTHONUNBUFFERED=1

# ============================================================================
# 端口配置
# ============================================================================
# 生产环境使用标准端口
BACKEND_PORT=5000
FRONTEND_PORT=3000
NGINX_PORT=80

# 本地开发建议使用非标准端口避免冲突
# BACKEND_PORT=5001
# FRONTEND_PORT=3001
# NGINX_PORT=8080

# ============================================================================
# Docker 配置
# ============================================================================
COMPOSE_PROJECT_NAME=dimensio

# Python 版本
PYTHON_VERSION=3.9

# ============================================================================
# 功能开关
# ============================================================================
# Docker 镜像加速（中国服务器建议启用）
USE_DOCKER_MIRROR=true

# 热重载（仅本地开发启用）
ENABLE_HOT_RELOAD=false

# ============================================================================
# 日志配置
# ============================================================================
# 日志级别: DEBUG, INFO, WARNING, ERROR, CRITICAL
# 生产环境: INFO
# 开发环境: DEBUG
LOG_LEVEL=INFO
